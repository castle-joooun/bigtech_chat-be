apiVersion: v1
kind: Secret
metadata:
  name: bigtech-chat-spring-secrets
type: Opaque
data:
  # NOTE: In production, use external secrets management
  # (AWS Secrets Manager, HashiCorp Vault, Sealed Secrets, etc.)
  # These are placeholder values - base64 encoded

  # MySQL credentials
  # echo -n "bigtech" | base64
  MYSQL_USERNAME: Yml1dGVjaA==
  # echo -n "CHANGE_ME_IN_PRODUCTION" | base64
  MYSQL_PASSWORD: Q0hBTkdFX01FX0lOX1BST0RVQ1RJT04=

  # MongoDB credentials
  # echo -n "root" | base64
  MONGODB_USERNAME: cm9vdA==
  # echo -n "CHANGE_ME_IN_PRODUCTION" | base64
  MONGODB_PASSWORD: Q0hBTkdFX01FX0lOX1BST0RVQ1RJT04=

  # Redis password (empty by default)
  REDIS_PASSWORD: ""

  # JWT Secret Key
  # echo -n "your-production-256-bit-secret-key-here-for-hs256" | base64
  JWT_SECRET_KEY: eW91ci1wcm9kdWN0aW9uLTI1Ni1iaXQtc2VjcmV0LWtleS1oZXJlLWZvci1oczI1Ng==
